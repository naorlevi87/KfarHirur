# Timeline Feature Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a pannable 2D SVG map with a winding road and 9 nodes — a spatial, zoomable timeline of Kfar Hirur's story.

**Architecture:** A `motion.div` world-container (Framer Motion `drag`) holds a 3000×2200 SVG with the road and nodes. On node tap, we programmatically animate scale + position to zoom toward the tapped node (revealing sub-items via `minScale` threshold), and show a preview card via `AnimatePresence`. Data is a flat array — no hierarchy; zoom reveals nearby density, not children.

**Tech Stack:** React 19, Vite, Framer Motion (`motion`, `useMotionValue`, `animate`, `AnimatePresence`), CSS custom properties (`var(--road)`, `var(--accent)`).

---

## File Map

| Action | Path | Responsibility |
|--------|------|----------------|
| Create | `src/features/timeline/timelineData.js` | Flat items array — canvas coords, minScale, naor/shay content |
| Create | `src/features/timeline/TimelineFeature.jsx` | Root component: owns pan/zoom state, dispatches to canvas + preview |
| Create | `src/features/timeline/TimelineCanvas.jsx` | Framer `drag` world container + SVG shell |
| Create | `src/features/timeline/TimelineRoad.jsx` | SVG path (glow + dashed centerline) |
| Create | `src/features/timeline/TimelineNode.jsx` | Single SVG node (circle + label, size variants) |
| Create | `src/features/timeline/TimelinePreview.jsx` | Fixed-position preview card with AnimatePresence |
| Create | `src/features/timeline/TimelineFeature.css` | Feature-scoped styles; no hardcoded colors |
| Modify | `src/pages/timeline/TimelinePage.jsx` | Replace placeholder with `<TimelineFeature />` |
| Modify | `src/styles/globals.css` | Add `--road` + `--road-glow` tokens (naor/shay) |

---

## Task 1: Add CSS tokens for road color

**Files:**
- Modify: `src/styles/globals.css`

No test suite — verify by opening dev server and switching consciousness modes.

- [ ] **Step 1: Add `--road` and `--road-glow` tokens to globals.css**

In `src/styles/globals.css`, inside the `.main-layout` block (after `--glow: ...`), add:

```css
  --road: #7c6fcd;
  --road-glow: rgba(124, 111, 205, 0.08);
```

In the `.main-layout[data-consciousness-mode="shay"]` block (after `--glow: ...`), add:

```css
  --road: #e07a5f;
  --road-glow: rgba(224, 122, 95, 0.08);
```

- [ ] **Step 2: Commit**

```bash
git add src/styles/globals.css
git commit -m "feat(timeline): add --road and --road-glow CSS tokens"
```

---

## Task 2: Data layer — flat items array

**Files:**
- Create: `src/features/timeline/timelineData.js`

- [ ] **Step 1: Create `src/features/timeline/timelineData.js`**

```js
// src/features/timeline/timelineData.js
// Flat array of all timeline items — L0 nodes (minScale:0) + sub-items (minScale:2.0+).
// Each item has canvas coordinates (x,y), visibility threshold (minScale),
// size variant, and naor/shay content objects.
// Canvas size: 3000 x 2200.

export const CANVAS_W = 3000;
export const CANVAS_H = 2200;

export const items = [
  // ── L0 NODES — always visible ──────────────────────────────────────────────

  {
    id: 'atlit',
    x: 1400, y: 1900,
    minScale: 0,
    size: 'standard',
    naor: {
      tag: '2013–2015',
      title: 'עתלית',
      text: 'הזולה על חוף הים. DMT, מדורה, סופה. שני אנשים שעדיין לא יודעים מה הם עושים ביחד — אבל יודעים שהם עושים.',
    },
    shay: {
      tag: '2013–2015',
      title: 'עתלית',
      text: 'הים בלילה. אש. ריח של מלח ועשן. לא ידענו כלום, אבל הכל היה שם כבר — בזולה, ברוח, בעיניים זה של זה.',
    },
  },

  {
    id: 'einkemunim',
    x: 2060, y: 1700,
    minScale: 0,
    size: 'standard',
    naor: {
      tag: '2014–2015',
      title: 'עין כמונים',
      text: 'חוות גבינות עיזים בצפון. ניהול ראשון משותף. הבנו שאנחנו יודעים לעשות את זה — ושאנחנו עושים את זה אחרת.',
    },
    shay: {
      tag: '2014–2015',
      title: 'עין כמונים',
      text: 'עיזים, גבינה, צפון. בפעם הראשונה ניהלנו ביחד — ולא היה ויכוח אחד על מה "צריך" לעשות. רק עשינו.',
    },
  },

  {
    id: 'manuela',
    x: 2340, y: 1338,
    minScale: 0,
    size: 'standard',
    naor: {
      tag: '2015',
      title: 'מנואלה, זכרון',
      text: 'מסעדה איטלקית. שי טבח, אני מלצר. אחרי משמרת ישבנו על הגדר. "מסעדה שנתיים מהיום." עברו שבעה חודשים.',
    },
    shay: {
      tag: '2015',
      title: 'מנואלה, זכרון',
      text: 'ישבנו על הגדר אחרי משמרת. אני רק פתחתי את הפה ואמרתי את זה — ונאור כבר ענה "כן". בלי להסס שנייה.',
    },
  },

  {
    id: 'tlv',
    x: 1240, y: 638,
    minScale: 0,
    size: 'standard',
    naor: {
      tag: '2016',
      title: 'למה אנחנו מחכים?',
      text: '"נאור, למה אנחנו מחכים?" — שי חזר ממשמרת, שאל אותי. עזבנו את העבודות. 25 אלף כל אחד. שלושה חודשים לגרום לזה לקרות.',
    },
    shay: {
      tag: '2016',
      title: 'למה אנחנו מחכים?',
      text: 'חזרתי ממשמרת ב-3 בלילה ושאלתי. לא ידעתי מה אני מצפה שיגיד. הוא אמר "לכלום". קמנו בבוקר ועזבנו.',
    },
  },

  {
    id: 'joz-open',
    x: 1602, y: 378,
    minScale: 0,
    size: 'key',
    naor: {
      tag: 'יולי 2017',
      title: "ג'וז ולוז נפתח",
      text: "גבולות 5, פלורנטין. אין תפריט. אין מחיר. אתם מחליטים. שלושה שולחנות, ארוחה אחת, כל ערב.",
    },
    shay: {
      tag: 'יולי 2017',
      title: "ג'וז ולוז נפתח",
      text: "הלילה הראשון — שישה אנשים ישבו ואכלו מה שהכנתי. שילמו מה שהחליטו. לא היה כלום חוץ מהרגע הזה.",
    },
  },

  {
    id: 'corona',
    x: 1400, y: 75,
    minScale: 0,
    size: 'large',
    naor: {
      tag: '2020',
      title: 'קורונה',
      text: "חזרנו לג'וז אחרי שלושה שבועות של שקט. לא ידענו אם יהיה למי לחזור. פתחנו משלוחים — ג'וז עד הבית.",
    },
    shay: {
      tag: '2020',
      title: 'קורונה',
      text: "שלושה שבועות שתיקה. ואז קמנו. כי הבית לא נסגר — הוא משתנה. פתחנו משלוחים וזה היה אחרת לגמרי, וזה היה בסדר.",
    },
  },

  {
    id: 'pinum',
    x: 420, y: 378,
    minScale: 0,
    size: 'standard',
    naor: {
      tag: '2021',
      title: '[לא] צו פינוי',
      text: "שלוש מילים שינו הכל. עברנו לתנופה 7. הג'וז חזק יותר עכשיו — בגלל שנאלצנו לבחור אותו מחדש.",
    },
    shay: {
      tag: '2021',
      title: '[לא] צו פינוי',
      text: "חשבנו שזהו. ואז הבנו שאין לנו ברירה אלא לרצות את זה יותר. עברנו לתנופה 7 ופתחנו מחדש, קצת אחרים.",
    },
  },

  {
    id: 'milchama',
    x: 818, y: 378,
    minScale: 0,
    size: 'key',
    naor: {
      tag: '7.10.2023',
      title: 'מלחמה',
      text: "7.10. כל מה שבנינו עמד בסכנה. חזרנו בכל זאת. כי אין ברירה אחרת — כי הג'וז הוא הסיבה להישאר.",
    },
    shay: {
      tag: '7.10.2023',
      title: 'מלחמה',
      text: "עצרנו. השתקנו. ואז פתחנו. כי הבית לא מתנצל על כך שהוא בית. כי ראו אותם — ואנחנו רוצים להמשיך לראות אנשים.",
    },
  },

  {
    id: 'now',
    x: 280, y: 100,
    minScale: 0,
    size: 'standard',
    naor: {
      tag: '2024–2026',
      title: 'עכשיו',
      text: "לשמור על הבית פתוח. לחזור לעבוד ביחד. לבנות משהו שיכול לראות עוד אנשים. הכביש חוזר.",
    },
    shay: {
      tag: '2024–2026',
      title: 'עכשיו',
      text: "חזרנו לעבוד יחד — אחרי שנים. האתר הזה הוא הדבר הראשון שבנינו ביחד מחדש. הכביש חוזר.",
    },
  },

  // ── SUB-ITEMS — revealed at zoom ≥ 2.0 ────────────────────────────────────

  {
    id: 'corona-lockdown',
    x: 1310, y: 130,
    minScale: 2.0,
    size: 'small',
    naor: {
      tag: 'מרץ 2020',
      title: 'סגר ראשון',
      text: "שלושה שבועות סגר. הג'וז שקט לגמרי. ניסינו להבין מה עושים.",
    },
    shay: {
      tag: 'מרץ 2020',
      title: 'סגר ראשון',
      text: "הבית השתתק. לא ידעתי אם לבכות על זה או לשמוח שיש רגע נשימה.",
    },
  },

  {
    id: 'joz-delivery',
    x: 1490, y: 130,
    minScale: 2.0,
    size: 'small',
    naor: {
      tag: 'אפריל 2020',
      title: "ג'וז עד הבית",
      text: "פתחנו משלוחים. אנשים קיבלו את האוכל בבית. זה לא היה אותו דבר — וזה עבד בצורה מדהימה.",
    },
    shay: {
      tag: 'אפריל 2020',
      title: "ג'וז עד הבית",
      text: "אנשים כתבו לנו: 'הארוחה שלכם הייתה הדבר הכי נורמלי השבוע שלי.' זה היה הכל.",
    },
  },

  {
    id: 'milchama-return',
    x: 750, y: 340,
    minScale: 2.0,
    size: 'small',
    naor: {
      tag: 'אוקטובר 2023',
      title: 'חזרנו',
      text: "שלושה שבועות עצרנו. חזרנו בחושך, לאט, ללא הכרזות. רק פתחנו.",
    },
    shay: {
      tag: 'אוקטובר 2023',
      title: 'חזרנו',
      text: "לא ידענו אם לחזור. ואז הבנו שהשאלה לא נכונה — כי אין 'לא לחזור' בלי לאבד את עצמנו.",
    },
  },

  {
    id: 'joz-no-menu',
    x: 1670, y: 340,
    minScale: 2.0,
    size: 'small',
    naor: {
      tag: 'יולי 2017',
      title: 'ללא תפריט',
      text: "ביום הראשון מישהו שאל 'מה אפשר להזמין?' אמרתי: 'מה שאתה רוצה לשלם.' הוא עמד שם חמש דקות.",
    },
    shay: {
      tag: 'יולי 2017',
      title: 'ללא תפריט',
      text: "תפריט הוא חוזה. אמרנו: אין חוזה. יש ארוחה, יש שולחן, יש שיחה. אתם מחליטים כמה שווה לכם.",
    },
  },
];
```

- [ ] **Step 2: Verify the data loads (quick sanity)**

Run:
```bash
cd d:/KfarHirurDrive/KfarHirurCom && npm run dev
```

Open browser console and paste:
```js
import('/src/features/timeline/timelineData.js').then(m => console.log(m.items.length))
```
Expected output: `13`

- [ ] **Step 3: Commit**

```bash
git add src/features/timeline/timelineData.js
git commit -m "feat(timeline): add flat items data with naor/shay content"
```

---

## Task 3: TimelineRoad — SVG path

**Files:**
- Create: `src/features/timeline/TimelineRoad.jsx`

- [ ] **Step 1: Create `src/features/timeline/TimelineRoad.jsx`**

```jsx
// src/features/timeline/TimelineRoad.jsx
// Renders the winding SVG road: wide glow stroke + dashed centerline.
// Colors from var(--road) and var(--road-glow) — change with consciousness mode.

export function TimelineRoad() {
  const d =
    'M 1400 1900' +
    ' C 1600 1820, 1850 1780, 2050 1700' +
    ' C 2250 1620, 2380 1500, 2340 1340' +
    ' C 2300 1180, 2100 1100, 1900 1060' +
    ' C 1700 1020, 1500 1040, 1360 960' +
    ' C 1220 880, 1160 760, 1240 640' +
    ' C 1320 520, 1500 480, 1600 380' +
    ' C 1700 280, 1680 160, 1560 100' +
    ' C 1440 40, 1280 60, 1160 130' +
    ' C 1040 200, 940 320, 820 380' +
    ' C 700 440, 540 440, 420 380' +
    ' C 300 320, 240 200, 280 100';

  return (
    <g>
      {/* atmosphere glow */}
      <path
        d={d}
        fill="none"
        stroke="var(--road)"
        strokeWidth={30}
        strokeLinecap="round"
        opacity={0.04}
      />
      {/* dashed centerline */}
      <path
        d={d}
        fill="none"
        stroke="var(--road)"
        strokeWidth={1.5}
        strokeDasharray="10 8"
        strokeLinecap="round"
        opacity={0.28}
      />
    </g>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/features/timeline/TimelineRoad.jsx
git commit -m "feat(timeline): add TimelineRoad SVG path component"
```

---

## Task 4: TimelineNode — single node

**Files:**
- Create: `src/features/timeline/TimelineNode.jsx`

Node sizing from spec:
- `standard`: r=11, glow r=24
- `key`: r=13, glow r=28
- `large` (corona): r=18, glow r=50, double ring + Framer glow filter
- `small` (sub-items): r=7, glow r=16

- [ ] **Step 1: Create `src/features/timeline/TimelineNode.jsx`**

```jsx
// src/features/timeline/TimelineNode.jsx
// Single timeline node rendered inside the SVG.
// Handles size variants (standard/key/large/small) and tap callback.

const SIZE_CONFIG = {
  standard: { r: 11, glowR: 24 },
  key:      { r: 13, glowR: 28 },
  large:    { r: 18, glowR: 50 },
  small:    { r: 7,  glowR: 16 },
};

// label offsets: place label to the right of the node by default
const LABEL_OFFSET_X = 14;
const LABEL_OFFSET_Y = 5;

export function TimelineNode({ item, mode, onTap }) {
  const { id, x, y, size = 'standard' } = item;
  const { r, glowR } = SIZE_CONFIG[size] ?? SIZE_CONFIG.standard;
  const content = item[mode] ?? item.naor;
  const isLarge = size === 'large';
  const isSmall = size === 'small';

  function handleClick(e) {
    e.stopPropagation();
    const rect = e.currentTarget.getBoundingClientRect();
    onTap(item, { x: rect.left + rect.width / 2, y: rect.top + rect.height / 2 });
  }

  return (
    <g
      className={`tl-node tl-node--${size}`}
      data-id={id}
      onClick={handleClick}
      style={{ cursor: 'pointer' }}
    >
      {/* glow halo */}
      {isLarge ? (
        <>
          <circle cx={x} cy={y} r={glowR} fill="none" stroke="var(--road)" strokeWidth={0.8} opacity={0.18} />
          <circle cx={x} cy={y} r={r} fill="var(--road)" fillOpacity={0.12} stroke="var(--road)" strokeWidth={2.5} filter="url(#tl-glow)" />
        </>
      ) : (
        <circle cx={x} cy={y} r={glowR} fill="var(--road)" fillOpacity={isSmall ? 0.03 : 0.04} />
      )}

      {/* main circle */}
      {!isLarge && (
        <circle
          cx={x} cy={y} r={r}
          fill="#0c0c1a"
          stroke="var(--road)"
          strokeWidth={isSmall ? 1 : 1.5}
          strokeOpacity={isSmall ? 0.35 : size === 'key' ? 0.55 : 0.28}
        />
      )}

      {/* label */}
      <text
        x={x + LABEL_OFFSET_X}
        y={y + LABEL_OFFSET_Y}
        fill="var(--road)"
        fillOpacity={isLarge ? 0.85 : isSmall ? 0.45 : 0.55}
        fontSize={isLarge ? 17 : isSmall ? 12 : 15}
        fontFamily="var(--font-family, sans-serif)"
        pointerEvents="none"
      >
        {content.title}
      </text>
    </g>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/features/timeline/TimelineNode.jsx
git commit -m "feat(timeline): add TimelineNode SVG component with size variants"
```

---

## Task 5: TimelinePreview — preview card

**Files:**
- Create: `src/features/timeline/TimelinePreview.jsx`

- [ ] **Step 1: Create `src/features/timeline/TimelinePreview.jsx`**

```jsx
// src/features/timeline/TimelinePreview.jsx
// Fixed-position preview card. Shown via AnimatePresence when a node is tapped.
// Positioned near the tapped node, clamped to viewport.
// Reads naor/shay content from item[mode].

import { motion } from 'framer-motion';

const CARD_W = 240;
const CARD_H = 180; // approximate, for clamping

export function TimelinePreview({ item, mode, screenPos, onClose, onDive }) {
  const content = item[mode] ?? item.naor;
  const hasSubItems = false; // Phase 2: detect sub-items in data

  // clamp card position so it stays in viewport
  const vpW = window.innerWidth;
  const vpH = window.innerHeight;
  let left = (screenPos?.x ?? vpW / 2) + 16;
  let top  = (screenPos?.y ?? vpH / 2) - 20;
  if (left + CARD_W > vpW - 8) left = (screenPos?.x ?? vpW / 2) - CARD_W - 16;
  if (left < 8) left = 8;
  if (top + CARD_H > vpH - 8) top = vpH - CARD_H - 8;
  if (top < 8) top = 8;

  return (
    <motion.div
      className="tl-preview"
      style={{ left, top }}
      initial={{ opacity: 0, scale: 0.88, y: 6 }}
      animate={{ opacity: 1, scale: 1, y: 0 }}
      exit={{ opacity: 0, scale: 0.88, y: 6 }}
      transition={{ type: 'spring', stiffness: 340, damping: 28 }}
      onClick={e => e.stopPropagation()}
    >
      <div className="tl-preview__tag">{content.tag}</div>
      <div className="tl-preview__title">{content.title}</div>
      <div className="tl-preview__text">{content.text}</div>

      {hasSubItems && (
        <button className="tl-preview__dive" onClick={onDive}>
          צלול עוד ↓
        </button>
      )}

      <button className="tl-preview__close" onClick={onClose} aria-label="סגור">
        ×
      </button>
    </motion.div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/features/timeline/TimelinePreview.jsx
git commit -m "feat(timeline): add TimelinePreview card with AnimatePresence-ready structure"
```

---

## Task 6: TimelineCanvas — pannable world

**Files:**
- Create: `src/features/timeline/TimelineCanvas.jsx`

The canvas is a `motion.div` with `drag` that holds the SVG. The SVG has a `<defs>` block with the glow filter. The canvas renders children (road + nodes) inside the SVG.

- [ ] **Step 1: Create `src/features/timeline/TimelineCanvas.jsx`**

```jsx
// src/features/timeline/TimelineCanvas.jsx
// Pannable world container. A motion.div with Framer drag holds the SVG.
// Accepts worldX, worldY, worldScale as MotionValues from the parent.
// Children are rendered inside the SVG element.

import { motion, useTransform } from 'framer-motion';
import { CANVAS_W, CANVAS_H } from './timelineData.js';

export function TimelineCanvas({ worldX, worldY, worldScale, children, onBgClick }) {
  // Build the CSS transform string from motion values
  // We use a wrapping motion.div for drag + scale, SVG fills it
  return (
    <motion.div
      className="tl-canvas"
      style={{
        x: worldX,
        y: worldY,
        scale: worldScale,
        transformOrigin: '0 0',
        width: CANVAS_W,
        height: CANVAS_H,
        position: 'absolute',
        top: 0,
        left: 0,
      }}
      drag
      dragMomentum={false}
      dragElastic={0.05}
      // very loose constraints — effectively free panning
      dragConstraints={{
        top: -(CANVAS_H * 2),
        left: -(CANVAS_W * 2),
        right: CANVAS_W,
        bottom: CANVAS_H,
      }}
    >
      <svg
        width={CANVAS_W}
        height={CANVAS_H}
        viewBox={`0 0 ${CANVAS_W} ${CANVAS_H}`}
        onClick={onBgClick}
        style={{ display: 'block', overflow: 'visible' }}
      >
        <defs>
          <filter id="tl-glow" x="-50%" y="-50%" width="200%" height="200%">
            <feGaussianBlur stdDeviation="6" result="b" />
            <feMerge>
              <feMergeNode in="b" />
              <feMergeNode in="SourceGraphic" />
            </feMerge>
          </filter>
        </defs>

        {/* subtle dot texture */}
        <defs>
          <pattern id="tl-dots" x="0" y="0" width="60" height="60" patternUnits="userSpaceOnUse">
            <circle cx="30" cy="30" r="0.8" fill="var(--road)" opacity="0.05" />
          </pattern>
        </defs>
        <rect width={CANVAS_W} height={CANVAS_H} fill="url(#tl-dots)" />

        {children}
      </svg>
    </motion.div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/features/timeline/TimelineCanvas.jsx
git commit -m "feat(timeline): add TimelineCanvas pannable world container"
```

---

## Task 7: TimelineFeature — root component

**Files:**
- Create: `src/features/timeline/TimelineFeature.jsx`

This is the orchestrator. It owns:
- `worldX`, `worldY`, `worldScale` — MotionValues for pan + zoom
- `zoomLevel` — React state (0 = L0, 1+ = zoomed)
- `activeNode` — item id of the zoomed-to node
- `previewItem` + `previewScreenPos` — what to show in preview card

Zoom math: to center node `(nx, ny)` at scale `S`, the world must be translated to:
```
worldX = vpW/2 - nx * S
worldY = vpH/2 - ny * S
```

(Because the world's transform origin is `0 0`, and the node at canvas position `(nx, ny)` lands at screen position `(nx * S + worldX, ny * S + worldY)`. We want that to equal viewport center.)

- [ ] **Step 1: Create `src/features/timeline/TimelineFeature.jsx`**

```jsx
// src/features/timeline/TimelineFeature.jsx
// Root timeline component. Owns pan/zoom state and orchestrates canvas + preview.

import { useEffect, useState } from 'react';
import { animate, AnimatePresence, useMotionValue } from 'framer-motion';
import { useAppContext } from '../../app/appState/useAppContext.js';
import { items, CANVAS_W, CANVAS_H } from './timelineData.js';
import { TimelineCanvas } from './TimelineCanvas.jsx';
import { TimelineRoad } from './TimelineRoad.jsx';
import { TimelineNode } from './TimelineNode.jsx';
import { TimelinePreview } from './TimelinePreview.jsx';
import './TimelineFeature.css';

const ZOOM_SCALE = 2.5;
const SPRING = { type: 'spring', stiffness: 200, damping: 30 };

export function TimelineFeature() {
  const { mode } = useAppContext();

  const worldX = useMotionValue(0);
  const worldY = useMotionValue(0);
  const worldScale = useMotionValue(1);

  const [zoomLevel, setZoomLevel] = useState(0);
  const [previewItem, setPreviewItem] = useState(null);
  const [previewScreenPos, setPreviewScreenPos] = useState(null);

  // Set initial pan so the canvas is roughly centered on the road
  useEffect(() => {
    const vpW = window.innerWidth;
    const vpH = window.innerHeight;
    // center on x=1300, y=900 — middle of the road's bounding box
    worldX.set(vpW / 2 - 1300);
    worldY.set(vpH / 2 - 900);
  }, [worldX, worldY]);

  function handleNodeTap(item, screenPos) {
    const vpW = window.innerWidth;
    const vpH = window.innerHeight;
    const targetX = vpW / 2 - item.x * ZOOM_SCALE;
    const targetY = vpH / 2 - item.y * ZOOM_SCALE;

    animate(worldX, targetX, SPRING);
    animate(worldY, targetY, SPRING);
    animate(worldScale, ZOOM_SCALE, SPRING);

    setZoomLevel(1);
    setPreviewItem(item);
    setPreviewScreenPos({ x: vpW / 2, y: vpH / 2 });
  }

  function handleBack() {
    animate(worldScale, 1, SPRING);
    // re-center on road midpoint
    const vpW = window.innerWidth;
    const vpH = window.innerHeight;
    animate(worldX, vpW / 2 - 1300, SPRING);
    animate(worldY, vpH / 2 - 900, SPRING);

    setZoomLevel(0);
    setPreviewItem(null);
    setPreviewScreenPos(null);
  }

  function handleBgClick() {
    if (previewItem) {
      setPreviewItem(null);
      setPreviewScreenPos(null);
    }
  }

  const visibleItems = items.filter(item =>
    zoomLevel === 0 ? item.minScale === 0 : true
  );

  return (
    <div className="tl-feature" data-mode={mode}>
      <TimelineCanvas
        worldX={worldX}
        worldY={worldY}
        worldScale={worldScale}
        onBgClick={handleBgClick}
      >
        <TimelineRoad />
        {visibleItems.map(item => (
          <TimelineNode
            key={item.id}
            item={item}
            mode={mode}
            onTap={handleNodeTap}
          />
        ))}
      </TimelineCanvas>

      <AnimatePresence>
        {previewItem && (
          <TimelinePreview
            key={previewItem.id}
            item={previewItem}
            mode={mode}
            screenPos={previewScreenPos}
            onClose={handleBack}
          />
        )}
      </AnimatePresence>

      {zoomLevel > 0 && (
        <button
          className="tl-back-btn"
          onClick={handleBack}
          aria-label="חזרה למפה"
        >
          ← חזרה
        </button>
      )}

      <div className="tl-hint" id="tl-hint" aria-hidden="true">
        גרור לשוטט · לחץ על נקודה
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/features/timeline/TimelineFeature.jsx
git commit -m "feat(timeline): add TimelineFeature root — pan/zoom state, node tap, back navigation"
```

---

## Task 8: CSS — feature styles

**Files:**
- Create: `src/features/timeline/TimelineFeature.css`

- [ ] **Step 1: Create `src/features/timeline/TimelineFeature.css`**

```css
/* src/features/timeline/TimelineFeature.css */
/* Timeline feature styles. No hardcoded colors — all via CSS custom properties. */

/* ── Root container ─────────────────────────────────────────────── */

.tl-feature {
  position: relative;
  flex: 1;
  min-height: 0;
  overflow: hidden;
  background: #07070e; /* near-black canvas bg — same in both modes */
  touch-action: none;
  cursor: grab;
}

.tl-feature:active {
  cursor: grabbing;
}

/* ── Hint text ──────────────────────────────────────────────────── */

.tl-hint {
  position: absolute;
  bottom: 28px;
  left: 50%;
  transform: translateX(-50%);
  font-size: 11px;
  color: rgba(255, 255, 255, 0.18);
  pointer-events: none;
  letter-spacing: 0.5px;
  white-space: nowrap;
  animation: tl-hint-fade 1s ease 4s forwards;
}

@keyframes tl-hint-fade {
  to { opacity: 0; }
}

/* ── Back button ────────────────────────────────────────────────── */

.tl-back-btn {
  position: absolute;
  top: 16px;
  right: 16px;
  font-size: 13px;
  color: var(--road);
  background: rgba(0, 0, 0, 0.55);
  border: 1px solid color-mix(in srgb, var(--road) 30%, transparent);
  border-radius: 8px;
  padding: 7px 14px;
  cursor: pointer;
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  z-index: 20;
  direction: rtl;
  transition: background 0.15s, border-color 0.15s;
}

.tl-back-btn:hover {
  background: rgba(0, 0, 0, 0.75);
}

.tl-back-btn:focus-visible {
  outline: 2px solid var(--focus-ring);
  outline-offset: 2px;
}

/* ── Preview card ───────────────────────────────────────────────── */

.tl-preview {
  position: fixed;
  width: 240px;
  background: rgba(10, 10, 20, 0.92);
  border: 1px solid color-mix(in srgb, var(--road) 28%, transparent);
  border-radius: 14px;
  padding: 18px 20px 16px;
  z-index: 100;
  direction: rtl;
  box-shadow: 0 16px 60px rgba(0, 0, 0, 0.7);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
}

.tl-preview__tag {
  font-size: 10px;
  color: var(--road);
  letter-spacing: 1px;
  margin-bottom: 6px;
  opacity: 0.9;
}

.tl-preview__title {
  font-size: 15px;
  color: #e8e4ff;
  font-weight: 500;
  margin-bottom: 10px;
  line-height: 1.3;
}

[data-mode="shay"] .tl-preview__title {
  color: #fff3e8;
}

.tl-preview__text {
  font-size: 12px;
  color: rgba(255, 255, 255, 0.45);
  line-height: 1.8;
  margin-bottom: 14px;
}

.tl-preview__dive {
  font-size: 11px;
  color: var(--road);
  background: none;
  border: 1px solid color-mix(in srgb, var(--road) 30%, transparent);
  border-radius: 6px;
  padding: 5px 12px;
  cursor: pointer;
  display: inline-block;
  margin-bottom: 4px;
  transition: background 0.15s;
}

.tl-preview__dive:hover {
  background: color-mix(in srgb, var(--road) 10%, transparent);
}

.tl-preview__dive:focus-visible {
  outline: 2px solid var(--focus-ring);
  outline-offset: 2px;
}

.tl-preview__close {
  position: absolute;
  top: 10px;
  left: 12px; /* RTL: left side */
  font-size: 18px;
  color: rgba(255, 255, 255, 0.3);
  background: none;
  border: none;
  cursor: pointer;
  line-height: 1;
  padding: 2px 6px;
  transition: color 0.15s;
}

.tl-preview__close:hover {
  color: rgba(255, 255, 255, 0.7);
}

.tl-preview__close:focus-visible {
  outline: 2px solid var(--focus-ring);
  outline-offset: 2px;
}

/* ── SVG node interaction ────────────────────────────────────────── */

.tl-node {
  transition: opacity 0.2s;
}

.tl-node:hover circle {
  filter: brightness(1.4);
}
```

- [ ] **Step 2: Commit**

```bash
git add src/features/timeline/TimelineFeature.css
git commit -m "feat(timeline): add feature CSS — canvas, preview card, back button, hint"
```

---

## Task 9: Wire up TimelinePage

**Files:**
- Modify: `src/pages/timeline/TimelinePage.jsx`

- [ ] **Step 1: Replace placeholder in `src/pages/timeline/TimelinePage.jsx`**

Replace the entire file content with:

```jsx
// src/pages/timeline/TimelinePage.jsx
// Thin page shell for the timeline route. Renders TimelineFeature full-viewport.

import { TimelineFeature } from '../../features/timeline/TimelineFeature.jsx';

export function TimelinePage() {
  return <TimelineFeature />;
}
```

- [ ] **Step 2: Commit**

```bash
git add src/pages/timeline/TimelinePage.jsx
git commit -m "feat(timeline): wire TimelinePage to TimelineFeature"
```

---

## Task 10: Manual verification

- [ ] **Step 1: Run dev server**

```bash
npm run dev
```

Navigate to `http://localhost:5173/timeline`

- [ ] **Step 2: Verify L0 map**
  - Dark canvas visible
  - Road visible (dashed + glow)
  - 9 nodes visible: עתלית (bottom-center), עין כמונים (right), מנואלה (far right), תל אביב (back left), ג׳וז נפתח (upper right), קורונה (top center, larger + glowing), [לא] צו פינוי (left), מלחמה (upper left), עכשיו (top left)
  - Sub-items NOT visible
  - Hint text fades after 4s
  - Canvas pans on drag
  - Consciousness switcher in header changes road color (indigo → coral)

- [ ] **Step 3: Verify L1 zoom**
  - Tap any node → canvas zooms in toward tapped node (spring animation)
  - Preview card appears near screen center, with correct tag + title + text
  - Sub-items appear (4 small nodes near corona, milchama, joz-open)
  - Tap background → preview dismisses (card stays zoomed)
  - "← חזרה" button visible in top-right → tap → zooms back to L0

- [ ] **Step 4: Verify consciousness mode**
  - Toggle Naor/Shay in header
  - Road color changes (indigo `#7c6fcd` ↔ coral `#e07a5f`)
  - Preview card text changes — different voice per mode
  - Back button border color follows road token

- [ ] **Step 5: Verify mobile (Chrome DevTools → iPhone 14 Pro viewport)**
  - Touch drag pans
  - Tap node opens preview
  - Preview fits within viewport (no overflow)
  - Back button reachable

- [ ] **Step 6: Run lint**

```bash
npm run lint
```

Fix any errors reported. Expected: 0 errors.

- [ ] **Step 7: Final commit**

```bash
git add -A
git commit -m "feat(timeline): MVP complete — panning map, 9 nodes, zoom+preview, consciousness mode"
```

---

## Phase 2 — Out of Scope for this Plan

These are explicitly NOT in this plan (per spec):
- Level 2 full item view (long text, photo, video, embeds)
- Admin backoffice for content editing
- Year labels appearing on zoom
- Zoom-out breadcrumb navigation
- `onDive` in `TimelinePreview` (wired as prop but `hasSubItems` is always `false`)
